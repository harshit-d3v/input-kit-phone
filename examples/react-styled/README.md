# React styled example

Minimal Vite app that styles `usePhoneInput` only (no default `PhoneInput` markup).

```bash
cd examples/react-styled
bun install
bun run dev
```

The Vite config aliases `@input-kit/phone` to the repository root for local development.
